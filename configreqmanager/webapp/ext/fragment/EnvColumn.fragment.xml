<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns:tnt="sap.tnt">
    <tnt:InfoLabel
        text="{EnvId}"
        renderMode="Narrow"
        colorScheme="{= ${path: 'EnvId', targetType: 'any'} === 'PRD' ? 1 : ${path: 'EnvId', targetType: 'any'} === 'QAS' ? 3 : 4 }"/>
</core:FragmentDefinition>
